# 05 - 文档收口与回归验收

状态：已实现。

## 目标

把本轮行为变化同步到开发文档和待排查记录，确保发布后的实现、文档、测试一致。

## 实施范围

- 更新 `DESIGN.md` 的工具契约、流程表、难度策略、状态文件说明。
- 更新 `产品迭代路线.md`，把学习画像作为今日推荐和长期学习的基座。
- 更新 `待排查问题.md`，标记已修、暂缓和仍需手动验证的问题。
- 增加本轮手动验收清单。

## 验收

- 文档不再描述旧的题后全菜单。
- 文档明确 summary 当前角色是学习画像，不是自动资料包改写。
- `npm run check` 与 `npm test` 通过。

