---
type: concept
course: 面向对象程序设计
name: bool
tags: [概念卡片, cpp, 类型]
---

# bool

[[bool]] 是 C++ 中的布尔类型，只有 `true` 和 `false` 两个逻辑值。

其他类型转换为 `bool` 时，通常 0 转换为 `false`，非 0 转换为 `true`。

关联课程：[[3.3 C++语言中类型]]

