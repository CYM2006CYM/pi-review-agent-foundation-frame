---
type: concept
course: 面向对象程序设计
name: decltype
tags: [概念卡片, cpp, 类型推导]
---

# decltype

[[decltype]] 是 C++ 中根据表达式推导类型的关键字。

例如 `decltype(a + b)` 表示表达式 `a + b` 的类型，常用于模板和复杂表达式类型推导。

关联课程：[[3.3 C++语言中类型]]

