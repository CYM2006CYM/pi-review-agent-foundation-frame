---
type: concept
course: 面向对象程序设计
name: free
tags: [概念卡片, C, cpp, 动态内存]
---

# free

`free` 是 C 语言中用于释放 `malloc` 分配内存的函数。

在 C++ 中，`new` 应与 `delete` 配对，`malloc` 应与 `free` 配对，不能混用。

关联课程：[[3.8 变量的存储与作用域]]

