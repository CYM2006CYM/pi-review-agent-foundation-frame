---
type: concept
course: 面向对象程序设计
name: malloc
tags: [概念卡片, C, cpp, 动态内存]
---

# malloc

`malloc` 是 C 语言中用于动态分配原始内存的函数。

在 C++ 面向对象程序中，通常优先使用 `new/delete` 或更现代的资源管理工具，而不是直接使用 `malloc/free`，因为 `malloc` 不会调用对象构造函数。

关联课程：[[3.8 变量的存储与作用域]]

