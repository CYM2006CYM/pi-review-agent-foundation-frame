---
type: concept
course: 面向对象程序设计
name: 前置自增
aliases: [前置++, prefix++]
tags: [cpp, 运算符重载]
---

# 前置自增

[[前置自增]] `++a` 的语义是先修改对象，再返回修改后的对象。

重载形式：

```cpp
A& operator++();
```

通常返回当前对象引用：

```cpp
return *this;
```

相关：[[后置自增]]、[[一元运算符重载]]、[[返回引用]]、[[10.4 一元运算符重载]]

