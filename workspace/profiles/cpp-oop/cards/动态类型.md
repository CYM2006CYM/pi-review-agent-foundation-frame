---
type: concept
course: 面向对象程序设计
name: 动态类型
tags: [cpp, 概念卡片, 类型, 多态]
---

# 动态类型

[[动态类型]] 是运行时对象的真实类型。

```cpp
Parent* p = new Child;
```

这里 `p` 的静态类型是 `Parent*`，但它指向的对象动态类型是 `Child`。虚函数调用会依据动态类型选择具体实现。

相关：[[18.4 虚函数的作用机制]]、[[静态类型]]、[[动态编联]]
