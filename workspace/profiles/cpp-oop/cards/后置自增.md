---
type: concept
course: 面向对象程序设计
name: 后置自增
aliases: [后置++, postfix++]
tags: [cpp, 运算符重载]
---

# 后置自增

[[后置自增]] `a++` 的语义是先保存旧值，再修改对象，最后返回修改前的旧值。

重载形式：

```cpp
A operator++(int);
```

其中 `int` 形参只用于区分前置和后置版本，通常不使用。

相关：[[前置自增]]、[[一元运算符重载]]、[[10.4 一元运算符重载]]

