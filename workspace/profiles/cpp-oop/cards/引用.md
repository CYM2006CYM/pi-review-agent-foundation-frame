---
type: concept
course: 面向对象程序设计
name: 引用
tags: [概念卡片, cpp, 类型]
---

# 引用

[[引用]]是 C++ 中对已有对象的[[别名]]机制。

例如 `int& r = x;` 表示 `r` 是 `x` 的引用。引用是 C++ 相对 C 语言新增的重要机制。

引用可以理解为给已有变量起别名。修改引用和修改原变量作用在同一个对象上。

引用必须初始化，绑定后可以像普通变量一样使用。引用常用于[[引用传参]]。

关联课程：[[3.3 C++语言中类型]]、[[3.4 C++语言中类型-枚举]]、[[4.3 引用]]
