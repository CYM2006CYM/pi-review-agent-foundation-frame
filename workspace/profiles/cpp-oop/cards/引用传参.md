---
type: concept
course: 面向对象程序设计
name: 引用传参
tags: [概念卡片, cpp, 引用, 函数参数]
---

# 引用传参

[[引用传参]]是在函数形参中使用引用类型，使形参成为实参对象的别名。

```cpp
void f(int& x) {
    x = 10;
}
```

调用 `f(a);` 后，函数内部对 `x` 的修改会作用到实参 `a`。

关联课程：[[4.3 引用]]

