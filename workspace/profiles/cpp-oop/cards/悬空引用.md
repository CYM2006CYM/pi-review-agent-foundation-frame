---
type: concept
course: 面向对象程序设计
name: 悬空引用
tags: [cpp, 引用, 生命周期, 错误]
---

# 悬空引用

[[悬空引用]] 指引用仍然存在，但它绑定的对象已经失效。

典型错误：

```cpp
int& f() {
    int x = 0;
    return x;
}
```

`x` 是普通局部变量，函数结束后销毁，返回它的引用会产生无效引用。

相关：[[返回引用]]、[[生命周期]]、[[局部变量]]、[[栈区]]、[[5.6 函数的返回]]

