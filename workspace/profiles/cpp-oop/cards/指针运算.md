---
type: concept
course: 面向对象程序设计
name: 指针运算
tags: [概念卡片, cpp, 指针, 数组]
---

# 指针运算

[[指针运算]]指对指针做加减操作。若 `p` 是 `T*`，则：

```cpp
p + n
```

表示向后移动 `n` 个 `T` 类型元素，底层地址偏移大致为 `n * sizeof(T)`。

数组下标 `a[k]` 可以理解为 `*(a + k)`。

关联课程：[[4.1 指针]]

