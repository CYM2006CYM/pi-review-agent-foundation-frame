---
type: concept
course: 面向对象程序设计
name: 析构函数
tags: [cpp, 类, 对象生命周期]
---

# 析构函数

[[析构函数]] 是对象销毁时自动调用的特殊成员函数。

```cpp
class A {
public:
    ~A();
};
```

特点：

- 名称为 `~类名`。
- 没有返回值。
- 没有参数。
- 不能重载。
- 不能写成 `const`。

相关：[[构造函数]]、[[缺省析构函数]]、[[对象生命周期]]、[[8.4 析构函数]]

