---
type: concept
course: 面向对象程序设计
name: 枚举
tags: [概念卡片, cpp, 类型]
---

# 枚举

[[枚举]]是用户自定义类型的一种，用于表示一组命名常量。

在 C++ 中可使用 `enum` 或更现代的 `enum class`。

传统 `enum` 使用方便，但枚举值容易进入外层作用域并造成命名冲突。`enum class` 是[[强类型枚举]]，类型安全更好。

关联课程：[[3.3 C++语言中类型]]、[[3.4 C++语言中类型-枚举]]、[[7.8 Card类的定义和实现]]
