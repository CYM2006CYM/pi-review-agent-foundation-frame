---
type: concept
course: 面向对象程序设计
name: 派生类访问控制
tags: [cpp, 继承, 访问控制, 高频考点]
status: 初稿
priority: high
---

# 派生类访问控制

[[派生类访问控制]] 指基类成员经过继承后，在派生类中呈现出的访问权限。

| 基类成员原权限 | public 继承 | protected 继承 | private 继承 |
|---|---|---|---|
| `public` | `public` | `protected` | `private` |
| `protected` | `protected` | `protected` | `private` |
| `private` | 不可直接访问 | 不可直接访问 | 不可直接访问 |

> [!important] 高频考点
> 基类 `private` 成员不是“没有继承到派生类对象中”，而是派生类代码不能直接访问。基类自己的成员函数仍然可以访问这些私有数据。

相关：[[公有继承]]、[[保护继承]]、[[私有继承]]、[[不可访问成员]]
