---
type: concept
course: 面向对象程序设计
name: 结构体
tags: [概念卡片, cpp, 类型]
---

# 结构体

[[结构体]]是 C/C++ 中用 `struct` 定义的自定义类型。

在 C++ 中，结构体也可以有成员函数等面向对象特性；其默认访问权限与类不同。

C++ 中 `struct` 默认访问权限是[[public]]，而 `class` 默认访问权限是[[private]]。

关联课程：[[3.3 C++语言中类型]]、[[3.4 C++语言中类型-枚举]]

