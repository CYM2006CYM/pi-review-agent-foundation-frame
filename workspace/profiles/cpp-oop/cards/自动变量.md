---
type: concept
course: 面向对象程序设计
name: 自动变量
tags: [概念卡片, cpp, 变量, 存储期]
---

# 自动变量

[[自动变量]]通常指普通局部变量。它在进入所在作用域时创建，离开作用域时自动销毁。

```cpp
void f() {
    int a = 0; // 普通局部变量，通常具有自动存储期
}
```

早期 C/C++ 中 `auto` 曾用于显式表示这种变量；现代 C++ 中 [[auto]] 主要用于[[类型推导]]。

关联课程：[[3.7 单一定义规则]]

