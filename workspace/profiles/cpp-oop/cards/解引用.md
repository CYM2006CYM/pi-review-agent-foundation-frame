---
type: concept
course: 面向对象程序设计
name: 解引用
aliases: [间接访问, dereference]
tags: [概念卡片, cpp, 指针, 运算符]
---

# 解引用

[[解引用]]指通过指针访问它所指向的对象。

```cpp
int value = 10;
int* p = &value;
*p = 20;
```

`*p = 20;` 修改的是 `p` 指向的对象，而不是指针变量 `p` 本身。

关联课程：[[4.1 指针]]

